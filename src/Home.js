const Home = () => {
    // Use state in React
    const handleClick = () => {
        
    }
    
    return (
        <div className="home">
            <h2>Hompage</h2>
            <button onClick={handleClick}>Click Me</button>
        </div>
    );
}

export default Home;